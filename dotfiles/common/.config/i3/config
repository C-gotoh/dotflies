# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 8
font pango:SourceCodePro 8,FontAwesome 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# kill focused window
bindsym $mod+Shift+q kill ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks

# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+h focus left ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks
bindsym $mod+j focus down ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks
bindsym $mod+k focus up ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks
bindsym $mod+l focus right ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks

# alternatively, you can use the cursor keys:
# bindsym $mod+Left focus left ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks
# bindsym $mod+Down focus down ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks
# bindsym $mod+Up focus up ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks
# bindsym $mod+Right focus right ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
# bindsym $mod+Shift+Left move left
# bindsym $mod+Shift+Down move down
# bindsym $mod+Shift+Up move up
# bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+Shift+b split h

# split in vertical orientation
bindsym $mod+Shift+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks

# focus the parent container
bindsym $mod+a focus parent ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks

# focus the child container
#bindsym $mod+d focus child



# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)

#============================
#       My Config           #
#============================

#== define workspace names (prefix numbers for sorting, those are removed in the bar)
set $ws1  "1:一"
set $ws2  "2:二"
set $ws3  "3:三"
set $ws4  "4:四"
set $ws5  "5:五"
set $ws6  "6:六"
set $ws7  "7:七"
set $ws8  "8:八"
set $ws9  "9:九"
set $ws10 "10:十"

#== switch to workspace
bindsym $mod+1 workspace $ws1 ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks
bindsym $mod+2 workspace $ws2 ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks
bindsym $mod+3 workspace $ws3 ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks
bindsym $mod+4 workspace $ws4 ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks
bindsym $mod+5 workspace $ws5 ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks
bindsym $mod+6 workspace $ws6 ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks
bindsym $mod+7 workspace $ws7 ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks
bindsym $mod+8 workspace $ws8 ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks
bindsym $mod+9 workspace $ws9 ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks
bindsym $mod+0 workspace $ws10 ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks

#== move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1 ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks
bindsym $mod+Shift+2 move container to workspace number $ws2 ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks
bindsym $mod+Shift+3 move container to workspace number $ws3 ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks
bindsym $mod+Shift+4 move container to workspace number $ws4 ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks
bindsym $mod+Shift+5 move container to workspace number $ws5 ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks
bindsym $mod+Shift+6 move container to workspace number $ws6 ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks
bindsym $mod+Shift+7 move container to workspace number $ws7 ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks
bindsym $mod+Shift+8 move container to workspace number $ws8 ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks
bindsym $mod+Shift+9 move container to workspace number $ws9 ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks
bindsym $mod+Shift+0 move container to workspace number $ws10 ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks

#== define colors
set_from_resource $darkred     color1  #cc241d
set_from_resource $red         color9  #fb4934
set_from_resource $darkgreen   color2  #000000
set_from_resource $green       color10 #000000
set_from_resource $darkyellow  color3  #000000
set_from_resource $yellow      color11 #000000
set_from_resource $darkblue    color4  #000000
set_from_resource $blue        color12 #000000
set_from_resource $darkmagenta color5  #000000
set_from_resource $magenta     color13 #000000
set_from_resource $darkcyan    color6  #000000
set_from_resource $cyan        color14 #000000
set_from_resource $darkwhite   color7  #000000
set_from_resource $white       color15 #000000

set $black       #282828
set $darkblack   #8ec07c
set $darkred   #f03f3f
set $transparent #000001a3
set $barbg #2D2D2D
set $darkyellow #ffaa00

#                       BORDER      BACKGROUND  TEXT        INDICATOR   CHILD_BORDER
client.focused          $red        $black      $darkwhite  $darkred  $darkyellow
client.unfocused        $black      $black      $white      $darkred  $darkwhite
client.focused_inactive $black      $black      $white      $darkred  $darkcyan
client.urgent           $darkred    $darkred    $black      $darkred    $darkred
client.background       $black


#== bar definition
bar {
        position top
        status_command SCRIPT_DIR=/usr/lib/i3blocks i3blocks
        strip_workspace_numbers yes
        #separator_symbol " "
        #separator_symbol " *"
        #separator_symbol " ❮"
        #separator_symbol "∗"
        separator_symbol " "
        i3bar_command i3bar -t
        colors {
            statusline         $white
            background         $barbg
            separator          $green

            #                  BORDER       BACKGROUND   TEXT
            focused_workspace  $barbg       $barbg       $red
            inactive_workspace $barbg       $barbg       $white
            active_workspace   $barbg       $barbg       $white
            urgent_workspace   $darkred     $darkred     $transparent
            binding_mode       $darkred     $darkred     $transparent
        }
}



#===== Audio ===============

#== alsa
#bindsym XF86AudioLowerVolume exec --no-startup-id amixer -c -1 set Master 2dB-
#bindsym XF86AudioRaiseVolume exec --no-startup-id amixer -c 0 set Master 2dB+

#== pulseaudio
# kill i3blocks with signal to update volume instantly
bindsym XF86AudioLowerVolume exec --no-startup-id pulseaudio-ctl down && pkill -SIGRTMIN+1 i3blocks
bindsym XF86AudioRaiseVolume exec --no-startup-id pulseaudio-ctl up && pkill -SIGRTMIN+1 i3blocks
bindsym XF86AudioMute exec --no-startup-id pamixer -t && pkill -SIGRTMIN+1 i3blocks
bindsym XF86AudioMicMute exec --no-startup-id amixer set Capture toggle && pkill -SIGRTMIN+1 i3blocks
bindsym $mod+m exec --no-startup-id pamixer -t && pkill -SIGRTMIN+1 i3blocks

#== music control (mpd)
bindsym $mod+z exec --no-startup-id ~/self/dotflies/scripts/campc toggle
bindsym $mod+q exec --no-startup-id ~/self/dotflies/scripts/campc prev
bindsym $mod+x exec --no-startup-id ~/self/dotflies/scripts/campc next

#===== Display ============

#== toggle between hdmi and internal screen / audio 
bindsym Control+Mod1+Shift+S exec --no-startup-id sh ~/script/togglescreen.sh
bindsym Control+Mod1+Shift+A exec --no-startup-id sh ~/script/togglesound.sh

#== backlight control
bindsym XF86MonBrightnessUp exec --no-startup-id light -A 5
bindsym XF86MonBrightnessDown exec --no-startup-id light -U 5

#== Random wallpaper

bindsym Control+Mod1+4 exec --no-startup-id feh --bg-fill --randomize ~/img/wall/
bindsym Control+Mod1+5 exec --no-startup-id feh --bg-fill --randomize ~/img/wall/anime/

#===== Applications =========

#== start dmenu (a program launcher)
bindsym $mod+d exec --no-startup-id rofi -show drun -modi "drun#run#window#calc" -sort && exec --no-startup-id pkill -SIGRTMIN+2 i3blocks

#== terminal
bindsym $mod+Return exec --no-startup-id alacritty && exec --no-startup-id pkill -SIGRTMIN+2 i3blocks

#== filebrowser
bindsym $mod+n exec --no-startup-id nemo --no-desktop 

#== spawn pavucontrol as floating
for_window [class="(?i)pavucontrol"] floating enable, move position mouse

#== screenshot
bindsym --release Print exec --no-startup-id "scrot -e 'mv $f ~/img/shot/'"
bindsym --release Shift+Print exec --no-startup-id "scrot -s -e 'mv $f ~/img/shot/'"

#===== i3 features =========

#== cycle workspaces
bindsym $mod+p workspace next ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks
bindsym $mod+i workspace prev ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks

#== tab between workspace
bindsym $mod+Tab workspace back_and_forth ; exec --no-startup-id pkill -SIGRTMIN+2 i3blocks

#== i3lock
bindsym Control+Mod1+Shift+L exec --no-startup-id i3lock-fancy -p -n ; exec --no-startup-id echo 'Locking session' | systemd-cat -p warning -t i3lock

#== sticky floating window (for music control f.e.)
bindsym $mod+Shift+S sticky toggle 
bindsym $mod+y fullscreen disable; floating enable; resize set 600 400; sticky enable; move window to position 1940 54

#== soundboard
set $mode_soundboard soundboard
mode "$mode_soundboard" {
    bindsym 0      exec mpv --fullscreen ~/Videos/meninchairs.webm
    bindsym Escape mode "default"
    }
bindsym XF86Tools mode "$mode_soundboard"

#====================
# i3-gaps + borders
#====================

#== hide edge borders smart
hide_edge_borders smart

new_window none
for_window [class="^.*"] border pixel 2

set $default_gaps_inner 10
set $default_gaps_outer 0

gaps inner 10
gaps outer 0
smart_gaps on

#== gaps control

set $mode_gaps [gaps] (o, i, 0, d)
set $mode_gaps_inner [inner gaps] (<Shift> +, -, 0, d)
set $mode_gaps_outer [outer gaps] (<Shift> +, -, 0, d)
bindsym $mod+Shift+g mode "$mode_gaps"
mode "$mode_gaps" {
    bindsym o      mode "$mode_gaps_outer"
    bindsym i      mode "$mode_gaps_inner"

    bindsym 0      mode "default", exec --no-startup-id i3-msg "gaps inner current set 0" && i3-msg "gaps outer current set 0"
    bindsym d      mode "default", exec --no-startup-id i3-msg "gaps inner current set $default_gaps_inner" && i3-msg "gaps outer current set $default_gaps_outer"

    bindsym Return mode "default"
    bindsym Escape mode "default"
}

mode "$mode_gaps_inner" {
    bindsym plus  gaps inner current plus 5
    bindsym minus gaps inner current minus 5
    bindsym 0     mode "default", gaps inner current set 0
    bindsym d     mode "default", gaps inner current set $default_gaps_inner

    bindsym Shift+plus  gaps inner all plus 5
    bindsym Shift+minus gaps inner all minus 5
    bindsym Shift+0     mode "default", gaps inner all set 0
    bindsym Shift+d     mode "default", gaps inner all set $default_gaps_inner

    bindsym Return mode "default"
    bindsym Escape mode "default"
}

mode "$mode_gaps_outer" {
    bindsym plus  gaps outer current plus 5
    bindsym minus gaps outer current minus 5
    bindsym 0     mode "default", gaps outer current set 0
    bindsym d     mode "default", gaps outer current set $default_gaps_outer

    bindsym Shift+plus  gaps outer all plus 5
    bindsym Shift+minus gaps outer all minus 5
    bindsym Shift+0     mode "default", gaps outer all set 0
    bindsym Shift+d     mode "default", gaps outer all set $default_gaps_outer

    bindsym Return mode "default"
    bindsym Escape mode "default"
}

#== set background image
exec --no-startup-id feh --bg-fill ~/img/wall/spacecity.jpg
